services:
  admin_dashboard:
    build: 
      context: .
      dockerfile: Dockerfile.multistage
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
